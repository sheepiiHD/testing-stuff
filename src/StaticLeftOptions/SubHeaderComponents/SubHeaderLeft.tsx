import { Button } from "@mui/material";

const StaticLeft = () => {
  return (
    <div>
      <Button variant="outlined">Create New Grouping</Button>
    </div>
  );
};

export default StaticLeft;
